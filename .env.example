# ============================================
# 森空岛自动签到系统 - 环境变量配置
# ============================================
# 复制此文件为 .env 并填入您的配置

# --------------------------------------------
# 应用配置
# --------------------------------------------
# APP_NAME="Skland Auto Sign"
# APP_VERSION="1.0.0"
# APP_DEBUG=false

# --------------------------------------------
# 数据库配置
# --------------------------------------------
# 数据库类型 (sqlite/postgresql)
DATABASE_TYPE=sqlite
# 数据库连接 URL
DATABASE_URL=sqlite:///data/skland.db

# --------------------------------------------
# 定时任务配置
# --------------------------------------------
# 明日方舟签到时间 (24小时制，格式: HH:MM)
SCHEDULER_ARKNIGHTS_SIGN_TIME=00:15
# 终末地签到时间 (24小时制，格式: HH:MM)
SCHEDULER_ENDFIELD_SIGN_TIME=00:20
# 时区
SCHEDULER_TIMEZONE=Asia/Shanghai
# 随机延迟（秒），避免同时请求
SCHEDULER_RANDOM_DELAY=300

# --------------------------------------------
# 日志配置
# --------------------------------------------
# 日志级别: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
# 日志目录
LOG_DIR=data/logs
# 日志保留时间
LOG_RETENTION=30 days
# 日志轮转时间
LOG_ROTATION=1 day

# --------------------------------------------
# Web API 配置
# --------------------------------------------
# 是否启用 Web 管理界面
WEB_ENABLED=false
# Web 服务监听地址
WEB_HOST=0.0.0.0
# Web 服务端口
WEB_PORT=8080

# --------------------------------------------
# 账号配置
# --------------------------------------------
# 方式1: 使用 JSON 格式（推荐，适合多账号）
# JSON 格式示例，注意引号需要转义
ACCOUNTS_JSON='[{"name":"账号1","enabled":true,"token":"your_token_here","remark":"主账号"},{"name":"账号2","enabled":true,"token":"another_token","remark":"小号"}]'

# 方式2: 使用单独的环境变量（适合少量账号）
# 账号1配置
# ACCOUNTS_1_NAME="账号1"
# ACCOUNTS_1_ENABLED=true
# ACCOUNTS_1_TOKEN=your_token_here
# ACCOUNTS_1_CRED=
# ACCOUNTS_1_CRED_TOKEN=
# ACCOUNTS_1_REMARK=主账号

# 账号2配置
# ACCOUNTS_2_NAME="账号2"
# ACCOUNTS_2_ENABLED=true
# ACCOUNTS_2_TOKEN=another_token
# ACCOUNTS_2_REMARK=小号

# --------------------------------------------
# 系统配置
# --------------------------------------------
# 时区（系统级）
TZ=Asia/Shanghai
# Python 不缓冲输出
PYTHONUNBUFFERED=1

# --------------------------------------------
# 配置说明
# --------------------------------------------
# 1. token 是森空岛的 access_token，用于在 cred 失效时自动刷新
# 2. cred 和 cred_token 是森空岛的登录凭证，有效期较短
# 3. 推荐只配置 token，程序会自动获取 cred 和 cred_token
# 4. 如果已通过其他方式获取到 cred 和 cred_token，也可以直接配置
# 5. 方式1（JSON）和方式2（单独变量）不能同时使用，优先使用 JSON
